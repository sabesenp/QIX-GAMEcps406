from pygame import *
class MainActor:
        def ___init___(self, this, health, edge):
                self.this = this
                self.health = health
                self.edge = edge

        def move(dx, dy):
                this = this.move(dx, dy)