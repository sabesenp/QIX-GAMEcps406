from pygame import *
class Enemy:
        def ___init___(self, this, dmg, edge, goal):
                self.this = this
                self.dmg = dmg
                self.edge = edge
                self.goal = (0, 0)
