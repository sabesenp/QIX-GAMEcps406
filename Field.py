from pygame import *
class Field:
        def __init__(self, center = Rect(0, 0, 0, 0), edge = Rect(0, 0, 0, 0)):
                self.name = "place the holder"
                self.center = center
                self.edge = edge